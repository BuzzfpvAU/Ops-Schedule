# Ops-Schedule — Project Status

> Agent-facing status for the taskz.id team operations schedule planner.

## Current State (2026-09-19)
- **Branch:** `main` at `1ad19d6` — V2 UI merged (see below)
- **Live:** https://taskz.id — workflow build deployed + verified 18 Sep.
- **V2 UI:** on `main` (`2f4e7f4`). **CONFIRMED NOT DEPLOYED as of 20 Sep.** `https://taskz.id/api/health` returned `{"status":"ok","timestamp":...}` with **no `frontends` key**, which only exists from `31c5838` — so prod is still serving 18 Sep code (`7a59548`) and every push since has failed to land. That is why `/v2` shows v1: the old code has no `/v2` route, so it falls to the v1 catch-all.
- **Suspected cause:** this branch changed `postinstall` and `build` — the two scripts the deploy runs — and the host only swaps the live symlink on a successful build, so a client-v2 install/build failure there would pin prod to the previous version silently. `2f4e7f4` restores `postinstall` byte-for-byte to its known-good form and makes the client-v2 build best-effort so it can never block a v1 deploy again. **Unproven** — nobody has seen the host's build log.
- **Next step:** re-check `/api/health`. If `frontends` is still absent after a deploy window, the pipeline itself is not running (watcher/webhook), not the build — fix that before touching code. Manual fallback: `cd ~/domains/taskz.id/nodejs && git pull && npm install && npm run build && touch tmp/restart.txt`
- **New plan:** AirPinpoint feature-parity plan — `docs/superpowers/plans/2026-09-11-airpinpoint-feature-parity.md`
- **Tests:** `npm test` (node --test) — 89 passing (server + both clients)
- **Front ends:** v1 at `/` (`client/`), V2 timeline UI at `/v2` (`client-v2/`). `npm run build` builds both.
- **Stack:** Express + better-sqlite3 + React/Vite + Leaflet; JWT httpOnly cookies, WebAuthn passkeys, Resend email
- **Prod:** Hostinger VPS, Passenger (Node 20), app at `~/domains/taskz.id/nodejs`, DB at `~/data/ops-schedule.db`, env via `.htaccess SetEnv`; npm at `/opt/alt/alt-nodejs20/root/bin`
- **Tracker:** `tracker/` (FindMy.py poller) built, prod key set — pending Apple sign-in + key export

## Workflow + allocation build (2026-09-18)
- **Design + plan:** `docs/superpowers/specs/2026-09-18-planning-workflow-design.md`, `docs/superpowers/plans/2026-09-18-planning-workflow-implementation.md`
- **Built (chunks 1–3):** readiness gates + gated status transitions (409 + admin override with reason, admin notifications); job-card workflow stepper + readiness panel + Needs-attention filter; equipment booking buffer pads (±1 day), cross-job conflict detection, ✓ Confirm allocation, kit templates (apply kit / save-as-kit on the job card)
- **Tests:** root `npm test` — 52 passing (server); client build clean
- **Deployed + verified (18 Sep):** serving `index-EvuUbu5b.js` (hash matches local build). Push auto-deployed via hbuilds — SSH not needed (and not available from the Mac). New endpoints live (`/api/equipment/kits` → 401 unauth, not 404).
- **Pending: chunks 4–6** (custody bridge, maintenance/utilisation, compliance reminders)

## V2 UI build (2026-09-19)
- **Design:** `docs/superpowers/specs/2026-09-19-v2-ui-design.md`
- **What:** new timeline front end in `client-v2/`, served at `/v2`. The v1 client at `/` is untouched — same API, same session cookie, link back from the V2 header.
- **Views:** Projects (jobs as gantt bars, grouped by state, planned vs rostered), Equipment (bookings + availability, home-base toggle, clash detection, travel-time pads), Team (people by state, leave/TOIL/notes, Unallocated-work group).
- **Zoom:** day / week / month — columns stay one day wide, only px-per-day changes (34 / 13 / 4).
- **Server:** one additive endpoint, `GET /api/equipment/bookings?start&end` (maps booked days back to the `job_equipment` assignment). No schema change, no migration.
- **Tests:** root `npm test` — 89 passing (37 new in `client-v2/test/`). Both client builds clean.
- **Verified in a browser** against a seeded DB: zoom levels, sticky axes, group collapse, TOIL add/remove, travel-time adjust with conflict warning, grouping toggles, 430px viewport. No console errors.
- **Not built:** view 4 (Grant to specify), drag/resize editing of bars, job allocation from the team view.
- **Deploy note:** `npm run build` now builds both clients; `postinstall` installs `client-v2` too.
- **Build hardening:** `scripts/fix-bin-perms.mjs` runs as a `prebuild` step for both clients and restores the `+x` bit on vite/esbuild, so the recurring manual `chmod` on deploy should no longer be needed. Tested by stripping the bits and rebuilding.
- **Rollback:** `git reset --hard 7a59548 && git push --force-with-lease origin main`. No schema migration ships with V2.

## Open Items
- [x] ~~**Deploy the mobile view**~~ — deployed 10 Sep (`2613010`); verified live 11 Sep (bundle `index-CrIvQEye.js`).
- [ ] Verify the individual view's infinite scroll on a real phone — never exercised, see HANDOFF.md
- [ ] AirTag go-live: enable Find My on buzzbot, `findmy decrypt` → `tracker/keys/`, run `findmy_login.py`, install launchd job
- [ ] `deploy` submodule dirty (modified `client/package-lock.json`, HEAD `-dirty`)
- [ ] Shared viewer creds (`view@auav.com.au`) hardcoded in `server/src/db.js` — plaintext in git history
- [ ] Local DB has demo rows (16 seeded locations + demo admin) — local only, prod untouched

## Deploy notes
- VPS: `cd ~/domains/taskz.id/nodejs && git reset --hard origin/main && npm install && npm run build && touch tmp/restart.txt`
- **Live app runs from `~/domains/taskz.id/hbuilds/current/nodejs/`** — Hostinger's hbuilds watcher auto-rebuilds the version when the source tree changes and swaps `current` symlink. The nodejs dir is the SOURCE; don't edit hbuilds/versions directly.
- npm not on SSH PATH → `export PATH=/opt/alt/alt-nodejs20/root/bin:$PATH`
- Prod DB backup lives in /tmp after deploys (`ops-schedule.db.bak-*`)
- vite/esbuild binaries lose +x after git ops → `chmod +x client/node_modules/vite/bin/vite.js client/node_modules/@esbuild/linux-x64/bin/esbuild`
- TRACKER_INGEST_KEY prod value is in `.htaccess` + `tracker/.env` (gitignored)

## Rituals
- Startup: agent-hub pull → git pull → this file → HANDOFF.md
- Shutdown: write HANDOFF.md, update agent-hub row, commit + push status
