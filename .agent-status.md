# Ops-Schedule — Project Status

> Agent-facing status for the taskz.id team operations schedule planner.

## Current State (2026-09-03)
- **Branch:** main (local == origin == prod)
- **Live:** https://taskz.id (HTTP 200)
- **Last commit:** 4ec9498 (2026-04-03) — collision bars fix
- **Stack:** Express + better-sqlite3 + React/Vite; JWT httpOnly cookies, WebAuthn passkeys, Resend email
- **Prod:** Hostinger VPS `~/domains/taskz.id/` (nodejs + public_html build)

## Open Items
- [ ] `deploy` submodule dirty (modified `client/package-lock.json`, HEAD `-dirty`)
- [ ] Untracked: `.claude/`, `.superpowers/`, `docs/superpowers/plans/2026-03-21-auth-rbac.md`, `docs/superpowers/specs/2026-03-22-shared-view-access-design.md`
- [ ] Shared viewer creds (`view@auav.com.au`) hardcoded in `server/src/db.js` — plaintext in git history
- [ ] Local DB (`server/data/ops-schedule.db` + WAL) has recent local activity — sync/deploy state to confirm

## Rituals
- Startup: agent-hub pull → git pull → this file → HANDOFF.md
- Shutdown: write HANDOFF.md, update agent-hub row, commit + push status
